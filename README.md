Sample minimal project using autotools, including GLib as a library and a separate test suite / directory.

Build:
```
autoreconf --install
./configure
make
```

Run the sample program:
```
src/repoexample
```

Run the test suite:
```
test/runtests
```
